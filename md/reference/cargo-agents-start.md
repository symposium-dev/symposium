# `symposium start`

Print Rust guidance and list available crate skills for the current workspace.

## Usage

```bash
symposium start
```

## Behavior

Prints general Rust guidance followed by a list of skills available for crates in the current workspace's dependency graph. Intended to be called by the agent at the start of a session.

This command is also available via the MCP server (`symposium mcp`).
