# Session state

Session state (activations, nudge history, prompt count, Rust file snapshots) is persisted as JSON files at `~/.symposium/sessions/<session-id>.json`, so state survives across hook invocations within a single coding session.
