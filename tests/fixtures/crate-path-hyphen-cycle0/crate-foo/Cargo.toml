[package]
name = "crate-foo"
version = "0.1.0"
edition = "2021"

# Chained edge to same crate with underscore spelling — cycle detection must normalize
[[package.metadata.symposium.skills]]
source.path = "skills"

[[package.metadata.symposium.plugins]]
source.cargo = "crate_foo"
