---
name: ws-only-skill
description: A skill that should only install when this plugin is in workspace context
---

This skill is gated by the default `.agents/skills` group which requires `workspace()`.
