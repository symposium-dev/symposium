name = "bp-plugin"
predicates = ["battery_pack(cli)"]

[[installations]]
name = "bp-checker"
# Script generated at runtime by the test — allows tests to vary
# behavior (exit 0 vs exit 1, witness JSON, etc).
executable = "$TEST_DIR/bp-checker.sh"

[[predicate]]
name = "battery_pack"
command = "bp-checker"

[[skills]]
source.path = "skills"
